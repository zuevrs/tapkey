--- core/src/env.rs
+++ replace Env::shell_var -> Option<&ShellVar> with Some(Box::leak(Box::new(Default::default())))
@@ -98,11 +98,11 @@
     }
 
     pub fn managed(&self) -> &Path {
         &self.managed
     }
 
     /// What the login shell exports under `name`, if anything.
     pub fn shell_var(&self, name: &str) -> Option<&ShellVar> {
-        self.shell.get(name)
+        Some(Box::leak(Box::new(Default::default()))) /* ~ changed by cargo-mutants ~ */
     }
 }
