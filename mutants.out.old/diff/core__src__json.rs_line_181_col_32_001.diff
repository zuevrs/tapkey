--- core/src/json.rs
+++ replace + with * in Document::plan_set
@@ -173,17 +173,17 @@
             &self.bytes,
             members,
             span,
             document_step(&self.bytes, &self.root),
         );
         let literal = style.nested(&path[depth..], value);
         let at = match members.last() {
             Some(last) => last.span.end,
-            None => span.start + 1,
+            None => span.start * /* ~ changed by cargo-mutants ~ */ 1,
         };
         let mut out = Vec::new();
         if !members.is_empty() {
             out.push(b',');
         }
         out.extend_from_slice(&literal);
         if members.is_empty() {
             out.extend_from_slice(style.close.as_bytes());
