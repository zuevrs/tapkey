--- core/src/fs.rs
+++ replace <impl FileSystem for FailOnce>::read -> io::Result<Option<Vec<u8>>> with Ok(None)
@@ -105,15 +105,15 @@
         self.attempted.push(path.to_path_buf());
         if self.should_fail() {
             return Err(io::Error::other("injected removal failure"));
         }
         self.real.remove(path)
     }
 
     fn read(&self, path: &Path) -> io::Result<Option<Vec<u8>>> {
-        self.real.read(path)
+        Ok(None) /* ~ changed by cargo-mutants ~ */
     }
 
     fn mode(&self, path: &Path) -> Option<u32> {
         self.real.mode(path)
     }
 }
