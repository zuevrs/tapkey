--- core/src/store.rs
+++ replace file_name -> String with String::new()
@@ -313,12 +313,10 @@
         } else {
             meta.len()
         };
     }
     Ok(total)
 }
 
 fn file_name(path: &Path) -> String {
-    path.file_name()
-        .map(|n| n.to_string_lossy().into_owned())
-        .unwrap_or_default()
+    String::new() /* ~ changed by cargo-mutants ~ */
 }
