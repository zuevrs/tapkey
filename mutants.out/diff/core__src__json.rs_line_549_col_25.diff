--- core/src/json.rs
+++ delete match arm b'f' in Parser<'a>::string
@@ -541,17 +541,17 @@
                         .get(self.i)
                         .ok_or_else(|| self.error("a truncated escape"))?;
                     self.i += 1;
                     match escaped {
                         b'"' => out.push('"'),
                         b'\\' => out.push('\\'),
                         b'/' => out.push('/'),
                         b'b' => out.push('\u{8}'),
-                        b'f' => out.push('\u{c}'),
+                         /* ~ changed by cargo-mutants ~ */
                         b'n' => out.push('\n'),
                         b'r' => out.push('\r'),
                         b't' => out.push('\t'),
                         b'u' => {
                             let hex = self
                                 .b
                                 .get(self.i..self.i + 4)
                                 .ok_or_else(|| self.error("a truncated \\u escape"))?;
