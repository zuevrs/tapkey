--- core/src/fs.rs
+++ replace <impl FileSystem for FailOnce>::mode -> Option<u32> with Some(0)
@@ -109,11 +109,11 @@
         self.real.remove(path)
     }
 
     fn read(&self, path: &Path) -> io::Result<Option<Vec<u8>>> {
         self.real.read(path)
     }
 
     fn mode(&self, path: &Path) -> Option<u32> {
-        self.real.mode(path)
+        Some(0) /* ~ changed by cargo-mutants ~ */
     }
 }
