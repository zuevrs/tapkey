{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The floating surfaces fit their content: the panel and the HUD resize to what they drew. set-size is the one window permission beyond the default set; everything else the surfaces do (hide, show, focus) is already in core:window:default.",
  "windows": ["*"],
  "permissions": ["core:window:allow-set-size"]
}
