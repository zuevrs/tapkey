#!/bin/sh
# The two failures that tell you nothing about your change: formatting, and a lint the compiler
# would have caught. Enable with: git config core.hooksPath .githooks
set -e
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
