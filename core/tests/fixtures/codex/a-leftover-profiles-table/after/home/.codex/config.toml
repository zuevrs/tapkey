model = "glm-5.3"
model_provider = "tapkey-zai"

# Dead weight that still type-checks: nothing reads it, and a table named `tapkey`
# would sabotage a later `codex --profile tapkey`.
[profiles.tapkey]
model = "something-else"

[model_providers.tapkey-zai]
name = "Z.ai"
base_url = "https://api.z.ai/api/v1"
wire_api = "responses"

[memories]
extract_model = "glm-4.6-air"
consolidation_model = "glm-4.6-air"
